# CONTEXT — дайджест проекта (читать первым, вместо PLAN.md и DESIGN.md)

> Назначение: дать новому чату рабочий контекст за один короткий файл.
> `PLAN.md` (1129 строк) и `DESIGN.md` (~1000) **не читаются целиком** — только нужный блок
> поиском по номеру пункта (`C3.11`) или по имени раздела (`§3.3`, `§4.2`).
> Этот файл обновляется, когда меняется структура или закрывается блок плана.
> Стартовый промт нового чата: [`tasks/_PROMPT.md`](./tasks/_PROMPT.md).

---

## 1. Что за продукт

Лаунчер компьютерного клуба «IMBA». Игрок садится за ПК → видит экран лога → входит →
получает оболочку с HUD сессии (остаток времени, кошелёк) → домашний экран с карточками
(сессия, квесты, Battle Pass, бар, лидерборд, «клуб сейчас», турнир) → библиотека игр →
заказ в бар → завершение сессии.

Три поверхности: **member** (Этап 1), **guest / PostPaid** (Этап 2), **админка** (Этап 3).
До Этапа 4 бэкенда нет — весь data-слой это моки, повторяющие будущий контракт.

## 2. Стек

Next.js 16 App Router · React 19 · **Tailwind v4** (нет `tailwind.config`; тема и материалы —
в `@theme` внутри `app/globals.css`) · zustand (срезы) · SWR через свой `hooks/use-api.ts` ·
framer-motion · `@base-ui/react` · sharp (ассеты). Пакетный менеджер — **pnpm**.

## 3. Документы

| Файл | Роль |
|---|---|
| `CONTEXT.md` | этот дайджест |
| `PLAN.md` | порядок работ и чекбоксы. §0.2 правила, §0.3 DoD, §0.4 дизайн-чеклист, §0.4.1 спека `design:verify`, **§0.5 выбор модели и бюджет чата** |
| `DESIGN.md` | источник правды по стилю: токены (§1), типографика (§2), материалы и шкала глубины (§3, §3.3), неон и тиры (§4.2), движение (§4.4–4.5), витрина (§11), иконки (§14) |
| `MVP-CLIENT.md` | сценарии: что делаем и зачем |
| `API-CONTRACT.md` | контракт мок-API = будущий реальный |
| `AGENT-CONTRACT.md` | контракт AgentBridge (мост к Windows-ПК) |
| `OFFLINE-TIME.md` | **офлайн и время:** две ступени счётчика (§1), показание против дельты (§4), ассиметрия prepaid/postpaid (§6), границы (§9). Карта решений для блока `C2.13`–`C2.19` |
| `C1-12-HANDOFF.md`, `F9-HANDOFF.md` | передача контекста по прошлым блокам |
| `tasks/_TEMPLATE.md` | шаблон ТЗ для чата-исполнителя |

## 4. Карта кода

```
app/
  page.tsx            единственный вход, роутинг экранов через store
  globals.css         ВСЯ тема: токены, материалы, вейлы, колодцы, неон
  dev/kit|bus|agent|sfx   витрины (design system, шина, агент, звук)
components/
  lock-screen.tsx     ДИЗАЙН-ЭТАЛОН — любой новый экран сверяется с ним
  attract-mode.tsx    экран ожидания (слайдшоу)
  app-shell.tsx  session-manager.tsx  toaster.tsx  data-boundary.tsx
  ui/                 примитивы: Panel HudChip HudPlate SectionHeader Segmented
                      StatTile RingProgress Money Countdown Progress Slider
                      EmptyState ErrorState Modal Drawer Overlay NavRail Field …
  launcher/           экраны и карточки клиента: home-view session-hud top-bar
                      quests-card battle-pass-card bar-card club-now-card
                      leaderboard-card tournament-card promo-strip games-view
                      shop-view profile-view cart-drawer checkout-modal …
  auth/               registration qr-login password-recovery seat-taken …
  dev-kit/            секции витрины (единственное место, где T1+T2 рядом легально)
  realtime/           realtime-provider offline-banner
lib/
  mock/api/*.ts       мок-API по домену: session shop loyalty social catalog
                      profile promo hero events auth support support
  mock/db.ts          сид · mock/persist.ts сохранение
  store/slices/*.ts   auth session cart loyalty social notifications settings ui
  store/index.ts      useStore + селекторы (cartTotalCents, cartCount…)
  realtime/           mock-bus.ts events.ts (EVENT_INVALIDATES) admin-sim.ts copy.ts
  i18n/dictionaries/  en.ts ru.ts lt.ts — ключи обязаны совпадать
  types/*.ts          домены: session order loyalty catalog tab pass social …
  money.ts time.ts    ФОРМАТИРОВАНИЕ (см. §6) · icons.ts · utils.ts (cn)
  agent/bridge.ts     AgentBridge + mock-agent.ts
hooks/
  use-api.ts          useApi(key, fetcher) + useInvalidate(...prefixes)
  use-realtime.ts use-agent.ts use-sfx.ts use-reduced-motion.ts
  use-roving-focus.ts use-dismissable-layer.ts use-idle.ts …
scripts/
  verify-design.mjs verify-icons.mjs verify-assets.mjs   гейты (висят на prebuild)
  optimize-*.mjs generate-blur.mjs generate-sfx.mjs      сборка ассетов
```

## 5. Дизайн-запреты (падает CI)

| Нарушение | Правило | Замена |
|---|---|---|
| `bg-black` / `bg-black/NN` вне `globals.css` | R2 | роль по шкале глубины §3.3: `.well` `.well-shallow` `.well-deep` `.scrim` `.pill` `.pill-deep` |
| чёрный градиент в JSX (`from-black/85`, `rgba(0,0,0,·)`, `rgba(10,10,12,·)`) | R1 | утилита вейла в `globals.css`: `.veil-login-h/-v/-floor`, `.veil-attract-*`, `.scanlines` |
| два T1 (`.neon-ring`/`.neon-edge` без `-static`) в одном файле | R3 | один T1 на кадр = главное действие; остальное `-static` (T2) или без неона (T3) |
| прямой импорт `lucide-react` | `icons:verify` | только `lib/icons.ts` |

Цветные градиенты (`rgba(229,53,43,·)`, `var(--primary)`, обложки, тосты) — **не нарушение**.
Исключения из T1: `components/dev-kit/**`, `attract-mode.tsx`.

Ещё запрещено (глазами, не скриптом): `bg-white`, `text-black`, фиолетовый, эмодзи как иконки,
`space-*` для отступов, margin-хаки вместо `gap-*`, радиусы вне `radius-sm/md/lg/xl` (3/6/10/16),
`full` только у пилюль и аватаров.

Обязательно: `app-ambient` + `hairline-grid` на фоне, панели через `glass`/`glass-strong`/`panel`,
`font-display` Manrope для заголовков, `font-sans` Inter для текста, `font-clock` для цифр,
`label-mono` для микро-лейблов, у каждой секции нумерованный `SectionHeader` (`01 / SESSION`),
primary `#e5352b`, анимации `framer-motion` 0.25–0.7s ease-out и отключаемы флагом
«Уменьшить анимации» + `prefers-reduced-motion`.

## 6. Данные, деньги, время

- Ни одной цифры в JSX. Всё через `lib/mock/api/*` → `useApi` → компонент.
- Деньги — **в центах**, тип `Cents`. Форматировани�� `formatEur` / `formatEurSigned` /
  `formatCoins` из `lib/money.ts`. Арифметика — тоже оттуда (`sumCents`, `mulCents`, `discountCents`).
- Время — `lib/time.ts`: `formatCountdown` (`HH:MM:SS`), `formatDuration`, `remainingSeconds`,
  `secondsUntil`, `serverNowMs`.
- **`lib/format.ts`, упомянутый в `PLAN.md §0.3`, не существует** — это `money.ts` + `time.ts`.
- Инвалидация после события: `EVENT_INVALIDATES` в `lib/realtime/events.ts` + `useInvalidate()`.
- Любой клиентский расчёт денег/минут помечается `// TRUST: сервер обязан пересчитать (Этап 4)`.

## 7. Текст

Только i18n, и сразу три словаря: `lib/i18n/dictionaries/{en,ru,lt}.ts`. Ключи обязаны совпадать
во всех трёх. Формулировки заданий/акций от админа (admin-authored) печатаются как есть и в
словари не идут. `aria-label` несёт контекст там, где видимая подпись повторяется
(три кнопки «Забрать» = три кнопки без названия).

## 8. Команды

```
pnpm dev              дев-сервер
pnpm design:verify    R1/R2/R3
pnpm icons:verify     импорты иконок
pnpm assets:verify    манифесты ассетов
pnpm build            гоняет все verify через prebuild
pnpm assets:build     пересборка ассетов (тяжёлый, sharp)
```

## 9. Модель под пункт — готовая таблица

> Та же метка стоит **прямо в `PLAN.md`** строкой под заголовком каждого блока — читая план,
> вы видите её сразу и сюда заходить не обязательно. Эта таблица — сводка на одном экране.

Два тира, третьего нет:

- **БЫСТРАЯ** = `v0 Mini` (или `Opus 5 Fast`). Спека есть, решений нет, эталон рядом.
- **СИЛЬНАЯ** = `Opus 5` (или `v0 Max`). Нужно решение, вкус или контракт.
- **2 ЧАТА** = сильная, и сразу планируем разведку + исполнение (§0.5 плана).

| Пункт | Модель | Почему |
|---|---|---|
| `C2.14` `C2.16` | 2 ЧАТА | идемпотентный отчёт и догон на реконнекте — прямо деньги и минуты |
| `C2.15` `C2.17` `C2.18` | СИЛЬНАЯ | ступень 2, потолок долга, монотонное показание — решения, а не механика |
| `C2.19` | БЫСТРАЯ | приёмка блока: сценарии + скрипты |
| `C3.11` | БЫСТРАЯ | сетка и высоты по готовой спеке + долг нумерации |
| `C3.12` | СИЛЬНАЯ | онбординг с подсветкой реальных элементов — новый паттерн и вкус |
| `C3.13` | БЫСТРАЯ | пустые состояния по образцу существующих карточек |
| `C4.1` | БЫСТРАЯ | сетка обложек, `GameCover`; обложки — отдельный прогон генерации |
| `C4.2` `C4.3` `C4.4` | БЫСТРАЯ | фильтры, поиск, карточка — механика по спеке |
| `C4.5` | СИЛЬНАЯ | детальная панель: новая крупная поверхность с нуля |
| `C4.6` `C4.7` `C4.8` | 2 ЧАТА | AgentBridge: шаги запуска, house-аккаунты, ошибки — контракт |
| `C4.9` | БЫСТРАЯ | избранное |
| `C4.10` | СИЛЬНАЯ | индикатор запущенной игры — живёт поверх всей оболочки |
| `C5.1`–`C5.12` | БЫСТРАЯ | экраны настроек: списки, тумблеры, значения из мок-API |
| `C5.13`–`C5.19` | 2 ЧАТА | FPS-оверлей: оверлей в игре, а не экран лаунчера |
| `C6` `C7` | БЫСТРАЯ | бар, кошелёк — данные и списки по спеке |
| `C8` | 2 ЧАТА | лояльность + Battle Pass, приоритет №1, много контрактов |
| `C9` `C10` | БЫСТРАЯ | друзья/пати, турниры |
| `C11` | СИЛЬНАЯ | помощь и связь с админом — тон и сценарии |
| `C12` `C13` | БЫСТРАЯ | бронирование, профиль |
| `C14` | СИЛЬНАЯ | завершение сессии — эмоционально важный экран |
| `C15` | БЫСТРАЯ | приёмка: чеклист + прогон скриптов |
| `G1` `G3` `G4` | БЫСТРАЯ | гость по готовым клиентским поверхностям |
| `G2` | 2 ЧАТА | открытый счёт (tab) — деньги и состояние |
| `G5` | БЫСТРАЯ | приёмка Этапа 2 |
| `A1` | 2 ЧАТА | каркас админки — новое приложение |
| `A2` `A3` | 2 ЧАТА | карта зала, сессии и касса — ядро админки |
| `A4`–`A10` | БЫСТРАЯ | CRUD-поверхности после того, как каркас стоит |
| `A11` | СИЛЬНАЯ | оформление клиентских экранов — чистый визуал |
| `A12` | БЫСТРАЯ | приёмка Этапа 3 |
| `B1` | 2 ЧАТА | реальный бэкенд: Neon + Drizzle + Better Auth |
| `B2` | 2 ЧАТА | Windows-агент |

Метки поставлены под **текущий** план. Если пункт на разведке оказался тяжелее — исполнитель
обязан сказать это первым сообщением и пометку в этой таблице поправить.

## 10. Где мы сейчас

Закрыто: **Этап 0** целиком (`F1`–`F9`, включая скрипт `design:verify`), **`C1`**, **`C2`**,
**`C3.1`–`C3.13`** — весь раздел `C3` (домашний экран) закрыт. Всего 111 принятых пунктов. Долг
нумерации секций на гостевой поверхности закрыт в `C3.11` (`useSectionIndices` в `home-view.tsx`).
Пустые состояния новичка (`C3.13`) завязаны на серверное поле `UserProfile.isNewcomer` — три
карточки (`continue-row`, `quests-card`, `club-now-card`) меняют текст, а не только вид.

**Открыт хвост `C2`: `C2.19`** (приёмка блока) — двухступенчатый учёт времени. `C2.13` (закрытая дверь
входа офлайн) **закрыт**: гейт `hooks/use-entry-gate.ts`, панель `components/auth/entry-offline.tsx`,
подключённый в `mutate()` набор `OFFLINE_ENTRY_BLOCKED` и двухпараметровый `RefusalReporter`
(`sales` vs `entry`). Заодно заложен договор C4.7/C4.8: `catalog.grantHouseAccount` в
`OFFLINE_BLOCKED`, `catalog.launchGame` — вне обоих списков.
`C2.14` (идемпотентный отчёт) **закрыт**: тело `{ anchorId, elapsedSinceAnchor }`, сервер берёт
`max` по сумме обеих половин счёта, анкор вращает **каждая** запись, двигающая время — включая сам
принятый `heartbeat` (`reanchorSession()` в `lib/mock/db.ts`, один на двух писателей строки).
`C2.15` (периодический отчёт) и `C2.16` (офлайн-накопление и догон на реконнекте) **закрыты**:
логика в `hooks/use-heartbeat.ts` на том же единственном такте (`F6.3`), защёлка `inFlight` —
модульная, одна на тик и на догон, порядок на ребре «отчёт → снапшот → `mutate`».
`C2.17` (офлайн-потолок postpaid) **закрыт**: `OFFLINE_TAB_CAP_SECONDS` в
`lib/store/slices/session.ts`, второй эффект в `time-warnings.tsx` (флаг обрыва **занимает**
баннерный `offline`, показание — `unreportedSeconds()`), панель
`components/launcher/offline-tab-cap.tsx` — ничего не закрывает и не списывает, «позвать админа»
вне `OFFLINE_BLOCKED`.
`C2.18` (показание не верит системным часам) **закрыт**: монотонная метка в том же слоте
наблюдения (`markSnapshotObserved(serverTime, atMs, monoMs)`, `monotonicSecondsSinceAnchor()` в
`lib/time.ts`), и монотонным стал не только отчёт, но и слагаемое `held` внутри skew-коррекции —
`promised` server-минус-server лечит статически уехавшие часы, а `held` это длительность, которую
перевод часов посреди визита ломал и на экране. `unreportedSeconds()` и postpaid-ветка `derive()`
считают одним и тем же пролётом; фолбэк на стенные часы остался там, где наблюдённого анкора нет.
Панель `components/dev-kit/kit-clock-skew.tsx` на `/dev/kit` (подменяет `Date.now` для вкладки —
единственный способ снять пункт глазами). Остальное заведено из
[`OFFLINE-TIME.md`](./OFFLINE-TIME.md).
Идёт **до `C4.6`–`C4.8`**: AgentBridge опирается на договор «офлайн можно запускать игру, но не
выдавать house-аккаунт», а тот стоит на корректном учёте времени.

**Следующий пункт: `C2.19`** (приёмка блока: сценарии подряд, `OFFLINE-TIME.md` §5 → пусто, §10 —
отмечен, счёт принятых пунктов здесь). `C4.1` (библиотека: сетка обложек, реальные
сгенерированные обложки вмес��о градиентов, `GameCover` с ленивой загрузкой) — после хвоста `C2`
либо параллельно, он времени не касается.

Что сломано в учёте времени **прямо сейчас**: ничего из списка карты — периодический отчёт и догон
на реконнекте закрыты в `C2.15`/`C2.16`, неидемпотентность — в `C2.14` (сервер берёт `max` по
показанию), офлайн-потолок postpaid — в `C2.17`, системные часы — в `C2.18`. Осталась приёмка
(`C2.19`) и известная цена монотонности: станция после реального suspend отчитается **меньше**
стенных часов — сторона недосчёта, следующий снапшот её закрывает.
Ступень 1 (локальные часы из анкоров) при этом **уже правильная** — её не переписываем.

Открытые долги: пункт «Как всё устроено» из меню аккаунта переезжает в раздел «Помощь» на `C11`;
`pnpm build` падает на `assets:verify` из-за наследия F9 (`public/__f9/`, `public/f9-freeze.js`) —
их надо либо объявить в §13, либо удалить. Долг `C3.12` (`tsc --noEmit` падал в
`first-run-tour.tsx` — проп `t` у `TourCard` и `titleKey`/`bodyKey` у `TourStep` были шире `TKey`)
**закрыт по ходу `C2.14`**: типы приведены к словарным, `tsc --noEmit` и `next build` чистые.

Дальше: `C4` (игры) → `C15` (приёмка Этапа 1).
